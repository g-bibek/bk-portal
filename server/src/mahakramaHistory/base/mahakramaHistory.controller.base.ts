/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { MahakramaHistoryService } from "../mahakramaHistory.service";
import { MahakramaHistoryCreateInput } from "./MahakramaHistoryCreateInput";
import { MahakramaHistoryWhereInput } from "./MahakramaHistoryWhereInput";
import { MahakramaHistoryWhereUniqueInput } from "./MahakramaHistoryWhereUniqueInput";
import { MahakramaHistoryFindManyArgs } from "./MahakramaHistoryFindManyArgs";
import { MahakramaHistoryUpdateInput } from "./MahakramaHistoryUpdateInput";
import { MahakramaHistory } from "./MahakramaHistory";
import { MahakramaFindManyArgs } from "../../mahakrama/base/MahakramaFindManyArgs";
import { Mahakrama } from "../../mahakrama/base/Mahakrama";
import { MahakramaWhereUniqueInput } from "../../mahakrama/base/MahakramaWhereUniqueInput";
@swagger.ApiBearerAuth()
export class MahakramaHistoryControllerBase {
  constructor(
    protected readonly service: MahakramaHistoryService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "MahakramaHistory",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: MahakramaHistory })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: MahakramaHistoryCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<MahakramaHistory> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "MahakramaHistory",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"MahakramaHistory"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: {
        ...data,

        student: {
          connect: data.student,
        },
      },
      select: {
        comment: true,
        createdAt: true,
        id: true,
        isDeleted: true,
        isMarkedCompleteByInstructor: true,
        isMarkedCompleteByStudent: true,

        student: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "MahakramaHistory",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: [MahakramaHistory] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(MahakramaHistoryFindManyArgs)
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<MahakramaHistory[]> {
    const args = plainToClass(MahakramaHistoryFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "MahakramaHistory",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        comment: true,
        createdAt: true,
        id: true,
        isDeleted: true,
        isMarkedCompleteByInstructor: true,
        isMarkedCompleteByStudent: true,

        student: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "MahakramaHistory",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: MahakramaHistory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: MahakramaHistoryWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<MahakramaHistory | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "MahakramaHistory",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        comment: true,
        createdAt: true,
        id: true,
        isDeleted: true,
        isMarkedCompleteByInstructor: true,
        isMarkedCompleteByStudent: true,

        student: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "MahakramaHistory",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: MahakramaHistory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: MahakramaHistoryWhereUniqueInput,
    @common.Body()
    data: MahakramaHistoryUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<MahakramaHistory | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "MahakramaHistory",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"MahakramaHistory"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          student: {
            connect: data.student,
          },
        },
        select: {
          comment: true,
          createdAt: true,
          id: true,
          isDeleted: true,
          isMarkedCompleteByInstructor: true,
          isMarkedCompleteByStudent: true,

          student: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "MahakramaHistory",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: MahakramaHistory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: MahakramaHistoryWhereUniqueInput
  ): Promise<MahakramaHistory | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          comment: true,
          createdAt: true,
          id: true,
          isDeleted: true,
          isMarkedCompleteByInstructor: true,
          isMarkedCompleteByStudent: true,

          student: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id/mahakrama")
  @nestAccessControl.UseRoles({
    resource: "MahakramaHistory",
    action: "read",
    possession: "any",
  })
  @ApiNestedQuery(MahakramaFindManyArgs)
  async findManyMahakrama(
    @common.Req() request: Request,
    @common.Param() params: MahakramaHistoryWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Mahakrama[]> {
    const query = plainToClass(MahakramaFindManyArgs, request.query);
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Mahakrama",
    });
    const results = await this.service.findMahakrama(params.id, {
      ...query,
      select: {
        createdAt: true,
        id: true,
        name: true,
        stepNumber: true,
        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post("/:id/mahakrama")
  @nestAccessControl.UseRoles({
    resource: "MahakramaHistory",
    action: "update",
    possession: "any",
  })
  async createMahakrama(
    @common.Param() params: MahakramaHistoryWhereUniqueInput,
    @common.Body() body: MahakramaHistoryWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      mahakrama: {
        connect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "MahakramaHistory",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"MahakramaHistory"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id/mahakrama")
  @nestAccessControl.UseRoles({
    resource: "MahakramaHistory",
    action: "update",
    possession: "any",
  })
  async updateMahakrama(
    @common.Param() params: MahakramaHistoryWhereUniqueInput,
    @common.Body() body: MahakramaWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      mahakrama: {
        set: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "MahakramaHistory",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"MahakramaHistory"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id/mahakrama")
  @nestAccessControl.UseRoles({
    resource: "MahakramaHistory",
    action: "update",
    possession: "any",
  })
  async deleteMahakrama(
    @common.Param() params: MahakramaHistoryWhereUniqueInput,
    @common.Body() body: MahakramaHistoryWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      mahakrama: {
        disconnect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "MahakramaHistory",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"MahakramaHistory"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
