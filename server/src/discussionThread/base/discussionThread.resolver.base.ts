/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import * as gqlUserRoles from "../../auth/gqlUserRoles.decorator";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { CreateDiscussionThreadArgs } from "./CreateDiscussionThreadArgs";
import { UpdateDiscussionThreadArgs } from "./UpdateDiscussionThreadArgs";
import { DeleteDiscussionThreadArgs } from "./DeleteDiscussionThreadArgs";
import { DiscussionThreadFindManyArgs } from "./DiscussionThreadFindManyArgs";
import { DiscussionThreadFindUniqueArgs } from "./DiscussionThreadFindUniqueArgs";
import { DiscussionThread } from "./DiscussionThread";
import { DiscussionForum } from "../../discussionForum/base/DiscussionForum";
import { DiscussionThreadService } from "../discussionThread.service";

@graphql.Resolver(() => DiscussionThread)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class DiscussionThreadResolverBase {
  constructor(
    protected readonly service: DiscussionThreadService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DiscussionThread",
    action: "read",
    possession: "any",
  })
  async _discussionThreadsMeta(
    @graphql.Args() args: DiscussionThreadFindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @graphql.Query(() => [DiscussionThread])
  @nestAccessControl.UseRoles({
    resource: "DiscussionThread",
    action: "read",
    possession: "any",
  })
  async discussionThreads(
    @graphql.Args() args: DiscussionThreadFindManyArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<DiscussionThread[]> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "DiscussionThread",
    });
    const results = await this.service.findMany(args);
    return results.map((result) => permission.filter(result));
  }

  @graphql.Query(() => DiscussionThread, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DiscussionThread",
    action: "read",
    possession: "own",
  })
  async discussionThread(
    @graphql.Args() args: DiscussionThreadFindUniqueArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<DiscussionThread | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "DiscussionThread",
    });
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.Mutation(() => DiscussionThread)
  @nestAccessControl.UseRoles({
    resource: "DiscussionThread",
    action: "create",
    possession: "any",
  })
  async createDiscussionThread(
    @graphql.Args() args: CreateDiscussionThreadArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<DiscussionThread> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "DiscussionThread",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"DiscussionThread"} creation is forbidden for roles: ${roles}`
      );
    }
    // @ts-ignore
    return await this.service.create({
      ...args,
      data: {
        ...args.data,

        discussionForum: args.data.discussionForum
          ? {
              connect: args.data.discussionForum,
            }
          : undefined,

        parentThread: args.data.parentThread
          ? {
              connect: args.data.parentThread,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => DiscussionThread)
  @nestAccessControl.UseRoles({
    resource: "DiscussionThread",
    action: "update",
    possession: "any",
  })
  async updateDiscussionThread(
    @graphql.Args() args: UpdateDiscussionThreadArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<DiscussionThread | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "DiscussionThread",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"DiscussionThread"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      // @ts-ignore
      return await this.service.update({
        ...args,
        data: {
          ...args.data,

          discussionForum: args.data.discussionForum
            ? {
                connect: args.data.discussionForum,
              }
            : undefined,

          parentThread: args.data.parentThread
            ? {
                connect: args.data.parentThread,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => DiscussionThread)
  @nestAccessControl.UseRoles({
    resource: "DiscussionThread",
    action: "delete",
    possession: "any",
  })
  async deleteDiscussionThread(
    @graphql.Args() args: DeleteDiscussionThreadArgs
  ): Promise<DiscussionThread | null> {
    try {
      // @ts-ignore
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => [DiscussionThread])
  @nestAccessControl.UseRoles({
    resource: "DiscussionThread",
    action: "read",
    possession: "any",
  })
  async childDiscussionThreads(
    @graphql.Parent() parent: DiscussionThread,
    @graphql.Args() args: DiscussionThreadFindManyArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<DiscussionThread[]> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "DiscussionThread",
    });
    const results = await this.service.findChildDiscussionThreads(
      parent.id,
      args
    );

    if (!results) {
      return [];
    }

    return results.map((result) => permission.filter(result));
  }

  @graphql.ResolveField(() => DiscussionForum, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DiscussionThread",
    action: "read",
    possession: "any",
  })
  async discussionForum(
    @graphql.Parent() parent: DiscussionThread,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<DiscussionForum | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "DiscussionForum",
    });
    const result = await this.service.getDiscussionForum(parent.id);

    if (!result) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.ResolveField(() => DiscussionThread, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DiscussionThread",
    action: "read",
    possession: "any",
  })
  async parentThread(
    @graphql.Parent() parent: DiscussionThread,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<DiscussionThread | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "DiscussionThread",
    });
    const result = await this.service.getParentThread(parent.id);

    if (!result) {
      return null;
    }
    return permission.filter(result);
  }
}
