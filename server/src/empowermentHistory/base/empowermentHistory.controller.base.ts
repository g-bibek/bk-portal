/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { EmpowermentHistoryService } from "../empowermentHistory.service";
import { EmpowermentHistoryCreateInput } from "./EmpowermentHistoryCreateInput";
import { EmpowermentHistoryWhereInput } from "./EmpowermentHistoryWhereInput";
import { EmpowermentHistoryWhereUniqueInput } from "./EmpowermentHistoryWhereUniqueInput";
import { EmpowermentHistoryFindManyArgs } from "./EmpowermentHistoryFindManyArgs";
import { EmpowermentHistoryUpdateInput } from "./EmpowermentHistoryUpdateInput";
import { EmpowermentHistory } from "./EmpowermentHistory";
import { User } from "../../user/base/User";
@swagger.ApiBearerAuth()
export class EmpowermentHistoryControllerBase {
  constructor(
    protected readonly service: EmpowermentHistoryService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "EmpowermentHistory",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: EmpowermentHistory })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: EmpowermentHistoryCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<EmpowermentHistory> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "EmpowermentHistory",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"EmpowermentHistory"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: {
        ...data,

        empowerment: data.empowerment
          ? {
              connect: data.empowerment,
            }
          : undefined,

        User: data.User
          ? {
              connect: data.User,
            }
          : undefined,
      },
      select: {
        createdAt: true,

        empowerment: {
          select: {
            id: true,
          },
        },

        id: true,
        isDeleted: true,
        updatedAt: true,

        User: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "EmpowermentHistory",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: [EmpowermentHistory] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(EmpowermentHistoryFindManyArgs)
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<EmpowermentHistory[]> {
    const args = plainToClass(EmpowermentHistoryFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "EmpowermentHistory",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        createdAt: true,

        empowerment: {
          select: {
            id: true,
          },
        },

        id: true,
        isDeleted: true,
        updatedAt: true,

        User: {
          select: {
            id: true,
          },
        },
      },
    });
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "EmpowermentHistory",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: EmpowermentHistory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: EmpowermentHistoryWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<EmpowermentHistory | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "EmpowermentHistory",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,

        empowerment: {
          select: {
            id: true,
          },
        },

        id: true,
        isDeleted: true,
        updatedAt: true,

        User: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "EmpowermentHistory",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: EmpowermentHistory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: EmpowermentHistoryWhereUniqueInput,
    @common.Body()
    data: EmpowermentHistoryUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<EmpowermentHistory | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "EmpowermentHistory",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"EmpowermentHistory"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          empowerment: data.empowerment
            ? {
                connect: data.empowerment,
              }
            : undefined,

          User: data.User
            ? {
                connect: data.User,
              }
            : undefined,
        },
        select: {
          createdAt: true,

          empowerment: {
            select: {
              id: true,
            },
          },

          id: true,
          isDeleted: true,
          updatedAt: true,

          User: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "EmpowermentHistory",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: EmpowermentHistory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: EmpowermentHistoryWhereUniqueInput
  ): Promise<EmpowermentHistory | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,

          empowerment: {
            select: {
              id: true,
            },
          },

          id: true,
          isDeleted: true,
          updatedAt: true,

          User: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
