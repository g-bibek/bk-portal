/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import * as gqlUserRoles from "../../auth/gqlUserRoles.decorator";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { CreateEmpowermentHistoryArgs } from "./CreateEmpowermentHistoryArgs";
import { UpdateEmpowermentHistoryArgs } from "./UpdateEmpowermentHistoryArgs";
import { DeleteEmpowermentHistoryArgs } from "./DeleteEmpowermentHistoryArgs";
import { EmpowermentHistoryFindManyArgs } from "./EmpowermentHistoryFindManyArgs";
import { EmpowermentHistoryFindUniqueArgs } from "./EmpowermentHistoryFindUniqueArgs";
import { EmpowermentHistory } from "./EmpowermentHistory";
import { User } from "../../user/base/User";
import { Empowerment } from "../../empowerment/base/Empowerment";
import { EmpowermentHistoryService } from "../empowermentHistory.service";

@graphql.Resolver(() => EmpowermentHistory)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class EmpowermentHistoryResolverBase {
  constructor(
    protected readonly service: EmpowermentHistoryService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "EmpowermentHistory",
    action: "read",
    possession: "any",
  })
  async _empowermentHistoriesMeta(
    @graphql.Args() args: EmpowermentHistoryFindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @graphql.Query(() => [EmpowermentHistory])
  @nestAccessControl.UseRoles({
    resource: "EmpowermentHistory",
    action: "read",
    possession: "any",
  })
  async empowermentHistories(
    @graphql.Args() args: EmpowermentHistoryFindManyArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<EmpowermentHistory[]> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "EmpowermentHistory",
    });
    const results = await this.service.findMany(args);
    return results.map((result) => permission.filter(result));
  }

  @graphql.Query(() => EmpowermentHistory, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "EmpowermentHistory",
    action: "read",
    possession: "own",
  })
  async empowermentHistory(
    @graphql.Args() args: EmpowermentHistoryFindUniqueArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<EmpowermentHistory | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "EmpowermentHistory",
    });
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.Mutation(() => EmpowermentHistory)
  @nestAccessControl.UseRoles({
    resource: "EmpowermentHistory",
    action: "create",
    possession: "any",
  })
  async createEmpowermentHistory(
    @graphql.Args() args: CreateEmpowermentHistoryArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<EmpowermentHistory> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "EmpowermentHistory",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"EmpowermentHistory"} creation is forbidden for roles: ${roles}`
      );
    }
    // @ts-ignore
    return await this.service.create({
      ...args,
      data: {
        ...args.data,

        empowerment: args.data.empowerment
          ? {
              connect: args.data.empowerment,
            }
          : undefined,

        User: args.data.User
          ? {
              connect: args.data.User,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => EmpowermentHistory)
  @nestAccessControl.UseRoles({
    resource: "EmpowermentHistory",
    action: "update",
    possession: "any",
  })
  async updateEmpowermentHistory(
    @graphql.Args() args: UpdateEmpowermentHistoryArgs,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<EmpowermentHistory | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "EmpowermentHistory",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(
      permission,
      args.data
    );
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new apollo.ApolloError(
        `providing the properties: ${properties} on ${"EmpowermentHistory"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      // @ts-ignore
      return await this.service.update({
        ...args,
        data: {
          ...args.data,

          empowerment: args.data.empowerment
            ? {
                connect: args.data.empowerment,
              }
            : undefined,

          User: args.data.User
            ? {
                connect: args.data.User,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => EmpowermentHistory)
  @nestAccessControl.UseRoles({
    resource: "EmpowermentHistory",
    action: "delete",
    possession: "any",
  })
  async deleteEmpowermentHistory(
    @graphql.Args() args: DeleteEmpowermentHistoryArgs
  ): Promise<EmpowermentHistory | null> {
    try {
      // @ts-ignore
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Empowerment, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "EmpowermentHistory",
    action: "read",
    possession: "any",
  })
  async empowerment(
    @graphql.Parent() parent: EmpowermentHistory,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<Empowerment | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Empowerment",
    });
    const result = await this.service.getEmpowerment(parent.id);

    if (!result) {
      return null;
    }
    return permission.filter(result);
  }

  @graphql.ResolveField(() => User, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "EmpowermentHistory",
    action: "read",
    possession: "any",
  })
  async user(
    @graphql.Parent() parent: EmpowermentHistory,
    @gqlUserRoles.UserRoles() userRoles: string[]
  ): Promise<User | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "User",
    });
    const result = await this.service.getUser(parent.id);

    if (!result) {
      return null;
    }
    return permission.filter(result);
  }
}
